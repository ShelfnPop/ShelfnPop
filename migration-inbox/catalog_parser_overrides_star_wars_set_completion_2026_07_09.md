# Star Wars Generic-Set Completion - 2026-07-09

## What this closes

This follow-up finishes the remaining reviewed Star Wars parser overrides that still used the plain `Star Wars` set label instead of a movie set.

## Updated UPCs

- `889698430180` -> `Yoda #124`
  - moved from generic `Star Wars` to `Star Wars: The Empire Strikes Back`
  - kept existing identity fields: `Metallic Gold`, `Walmart`
- `889698430203` -> `Darth Maul (Gold Metallic) #9`
  - moved from generic `Star Wars` to `Star Wars: The Phantom Menace`
  - kept existing identity fields: `Metallic Gold`, `Walmart`, `Vaulted`

## Evidence used

- Figure Realm's `Star Wars - Empire Strikes Back` checklist includes the `#124` Dagobah Yoda family, including gold variants.
- Figure Realm's `Star Wars - Phantom Menace` checklist includes the core `Darth Maul #9`, which supports placing the metallic-gold `#9` variant under the same movie set.

## Why this stays narrow

This batch only resolves missing movie-set placement. It does not rename the existing UPC identities or create a new specialty bucket.
