# Chewbacca #06 Parser Override - 2026-07-09

## Added durability

- `830395023243` -> `Chewbacca #06`
  - `franchise = Star Wars`
  - `set_name = Star Wars: The Empire Strikes Back`
  - `number = 06`

## Evidence used

- Figure Realm's `Star Wars - The Empire Strikes Back` checklist includes `Chewbacca #06`.
- Figure Realm's `Star Wars - A New Hope` checklist includes the separate row `Chewbacca (Hoth) #06`, which helps disambiguate the plain `Chewbacca #06` row.
- Funko's vault listing for `Chewbacca with Crossbow` confirms item number `2324` and box number `6`.

## Why this stays conservative

This override only locks the core identity and movie-set placement. It intentionally does not force a variant or exclusivity because secondary market listings around this UPC are inconsistent, and the stronger reviewed signal here is the base `Chewbacca #06` identity.
